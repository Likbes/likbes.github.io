import React from 'react';

const FormField = () => {

  const renderTemplate = () => {
    let formTemplate = null;

  };

  return (
    <div>
      {renderTemplate()}
    </div>
  );
};

export default FormField;
